#!/usr/bin/python3
# Write a function that checks if an IP address is valid (IPv4 format).

from utils import validate_ip
print('192.168.1.2 is a valid ip?')
print(validate_ip("192.168.1.2"))
# output: True
